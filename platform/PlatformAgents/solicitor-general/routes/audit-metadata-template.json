As of 1/26/2026, audit metadata is now embedded directly into OpenAPI/AsyncAPI via x-audit blocks.
This standalone template may be redundant, and marked as deprecated, 
but could still generate standalone audit manifests if needed.

{
  "audit_metadata": {
    "spec_bundle_version": "1.0.0",
    "generated_at": "{{ timestamp }}",
    "generated_by": "Solicitor-General Root Agent",
    "classification": "internal-regulated",
    "retention_policy": "7y-regulatory",
    "hashes": {
      "openapi_sha256": "{{ hash_openapi }}",
      "asyncapi_sha256": "{{ hash_asyncapi }}",
      "route_graph_sha256": "{{ hash_graph }}"
    },
    "provenance": [
      {
        "component": "Workflow-Builder",
        "version": "{{ builder_version }}",
        "commit": "{{ git_sha }}"
      },
      {
        "component": "TaskManager",
        "version": "{{ task_manager_version }}",
        "commit": "{{ git_sha }}"
      }
    ]
  }
}

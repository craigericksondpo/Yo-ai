{
  "$id": "https://yo-ai.ai/schemas/agent.register.input.schema.json",
  "title": "Agent.Register.Input",
  "description": "Input messageType for the Agent.Register capability.",
  "type": "object",
  "properties": {
    "identifier_ref": {
      "type": "string",
      "description": "Opaque reference to the agent being registered."
    },
    "subscriber_ref": {
      "type": "string",
      "description": "Reference to the subscriber associated with the agent."
    },
    "org_ref": {
      "type": "string",
      "description": "Reference to the organization associated with the agent."
    },
    "log_refs": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Log references relevant to the registration decision."
    }
  },
  "required": ["identifier_ref", "subscriber_ref", "log_refs"]
}

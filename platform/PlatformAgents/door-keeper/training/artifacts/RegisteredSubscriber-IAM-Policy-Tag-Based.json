// If instead your structure is:
// agent-registry/
//    agentA/
//    agentB/
// Then you can use a tag based condition:
{
  "Sid": "ReadAuthFoldersForOwnedAgents",
  "Effect": "Allow",
  "Action": "s3:GetObject",
  "Resource": "arn:aws:s3:::${bucket}/agent-registry/*/auth/*",
  "Condition": {
    "StringEquals": {
      "s3:ExistingObjectTag/owner": "${subscriber_id}"
    }
  }
}
// This is cleaner and scales better.

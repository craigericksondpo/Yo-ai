{
  "$id": "https://yo-ai.ai/schemas/integration-risk.evaluate.input.schema.json",
  "title": "Integration-Risk.Evaluate.Input",
  "description": "Profile-aware input for the Integration-Risk.Evaluate capability.",
  "type": "object",
  "properties": {
    "profile": {
      "$ref": "https://yo-ai.ai/schemas/profile.schema.json",
      "description": "Optional reference to the org or system profile to load."
    },
    "profilePatch": {
      "$ref": "https://yo-ai.ai/schemas/profile.patch.schema.json",
      "description": "Optional modifications to apply to the loaded profile."
    },
    "payload": {
      "type": "object",
      "description": "Capability-specific input fields for Integration-Risk.Evaluate.",
      "properties": {
        "org_ref": {
          "type": "string",
          "description": "Non-PII reference to the organization whose integration risk should be evaluated."
        }
      },
      "required": ["org_ref"]
    }
  },
  "required": ["payload"]
}
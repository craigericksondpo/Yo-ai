{
  "$id": "https://yo-ai.ai/schemas/purchase-receipt.generate.input.schema.json",
  "title": "Purchase-Receipt.Generate.Input",
  "description": "Profile-aware input for the Purchase-Receipt.Generate capability.",
  "type": "object",
  "properties": {
    "profile": {
      "$ref": "https://yo-ai.ai/schemas/profile.schema.json",
      "description": "Optional reference to the subject/org profile to load."
    },
    "profilePatch": {
      "$ref": "https://yo-ai.ai/schemas/profile.patch.schema.json",
      "description": "Optional modifications to apply to the loaded profile."
    },
    "payload": {
      "type": "object",
      "description": "Capability-specific input fields for Purchase-Receipt.Generate.",
      "properties": {
        "transactionId": { "type": "string" },
        "orderId": { "type": "string" },
        "amount": { "type": "number" },
        "currency": { "type": "string" }
      }
    }
  },
  "required": ["payload"]
}
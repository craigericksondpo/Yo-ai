{
  "$id": "https://yo-ai.ai/schemas/subscriber.register.input.schema.json",
  "title": "Subscriber.Register.Input",
  "description": "Profile-aware input for the Subscriber.Register capability.",
  "type": "object",
  "properties": {
    "profile": {
      "$ref": "https://yo-ai.ai/schemas/profile.schema.json",
      "description": "Optional reference to the subject/org profile to load."
    },
    "profilePatch": {
      "$ref": "https://yo-ai.ai/schemas/profile.patch.schema.json",
      "description": "Optional modifications to apply to the loaded profile."
    },
    "payload": {
      "type": "object",
      "description": "Capability-specific input fields for Subscriber.Register.",
      "properties": {
        "identifier": {
          "type": "string",
          "description": "Opaque identifier for the subscriber being registered."
        },
        "identifier_ref": {
          "type": "string",
          "description": "Reference to an existing subscriber record or external identity."
        },
        "log_refs": {
          "type": "array",
          "description": "Log references relevant to the subscriber registration decision.",
          "items": { "type": "string" }
        }
      },
      "required": ["identifier", "identifier_ref", "log_refs"]
    }
  },
  "required": ["payload"]
}
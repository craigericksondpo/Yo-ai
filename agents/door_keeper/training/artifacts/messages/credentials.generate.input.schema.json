{
  "$id": "https://yo-ai.ai/schemas/credentials.generate.input.schema.json",
  "title": "Credentials.Generate.Input",
  "description": "Profile-aware input for the Credentials.Generate capability.",
  "type": "object",
  "properties": {
    "profile": {
      "$ref": "https://yo-ai.ai/schemas/profile.schema.json",
      "description": "Optional reference to the subject/org profile to load."
    },
    "profilePatch": {
      "$ref": "https://yo-ai.ai/schemas/profile.patch.schema.json",
      "description": "Optional modifications to apply to the loaded profile."
    },
    "payload": {
      "type": "object",
      "description": "Capability-specific input fields for Credentials.Generate.",
      "properties": {
        "registration_identifier_ref": {
          "type": "string",
          "description": "Opaque reference to the registration record for which credentials are being generated."
        },
        "log_refs": {
          "type": "array",
          "description": "Log references relevant to the credential generation decision.",
          "items": { "type": "string" }
        }
      },
      "required": ["registration_identifier_ref", "log_refs"]
    }
  },
  "required": ["payload"]
}
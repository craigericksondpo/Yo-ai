{
  "$id": "https://yo-ai.ai/schemas/ip-portfolio.cluster.input.schema.json",
  "title": "IP-Portfolio.Cluster.Input",
  "description": "Profile-aware input for the IP-Portfolio.Cluster capability.",
  "type": "object",
  "properties": {
    "profile": {
      "$ref": "https://yo-ai.ai/schemas/profile.schema.json",
      "description": "Optional reference to the subject/org profile to load."
    },
    "profilePatch": {
      "$ref": "https://yo-ai.ai/schemas/profile.patch.schema.json",
      "description": "Optional modifications to apply to the loaded profile."
    },
    "payload": {
      "type": "object",
      "description": "Capability-specific input fields for IP-Portfolio.Cluster.",
      "properties": {
        "org_ref": { "type": "string" }
      },
      "required": ["org_ref"]
    }
  },
  "required": ["payload"]
}
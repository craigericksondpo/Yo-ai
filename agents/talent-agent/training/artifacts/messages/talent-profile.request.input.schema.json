{
  "$id": "https://yo-ai.ai/schemas/talent-profile.request.input.schema.json",
  "title": "Talent-Profile.Request.Input",
  "description": "Profile-aware input for the Talent-Profile.Request capability.",
  "type": "object",
  "properties": {
    "profile": {
      "$ref": "https://yo-ai.ai/schemas/profile.schema.json",
      "description": "Optional reference to the talent profile to load."
    },
    "profilePatch": {
      "$ref": "https://yo-ai.ai/schemas/profile.patch.schema.json",
      "description": "Optional modifications to apply to the loaded profile."
    },
    "payload": {
      "type": "object",
      "description": "Capability-specific input fields for Talent-Profile.Request.",
      "properties": {
        "requested_fields": {
          "type": "array",
          "description": "Fields needed for job or consulting workflows.",
          "items": { "type": "string" }
        },
        "profile_version": {
          "type": "string",
          "description": "Optional version of the profile requested."
        },
        "context": {
          "type": "object",
          "description": "Optional context from the requesting agent.",
          "additionalProperties": true
        }
      },
      "required": ["requested_fields"]
    }
  },
  "required": ["payload"]
}

{
  "$id": "https://yo-ai.ai/schemas/data-for-purpose.minimize.output.schema.json",
  "title": "Data-For-Purpose.Minimize.Output",
  "description": "Output messageType for the Data-For-Purpose.Minimize capability.",
  "type": "object",
  "properties": {
    "required_fields": {
      "type": "array",
      "description": "Fields determined to be necessary for the stated purpose.",
      "items": { "type": "string" }
    },
    "unnecessary_fields": {
      "type": "array",
      "description": "Fields that exceed the minimum necessary threshold.",
      "items": { "type": "string" }
    },
    "minimization_recommendations": {
      "type": "array",
      "description": "Recommended actions to reduce data exposure (e.g., remove, generalize, hash).",
      "items": { "type": "string" }
    },
    "compliance_rationale": {
      "type": "object",
      "description": "Explanation of how the minimization assessment aligns with regulatory frameworks.",
      "additionalProperties": true
    }
  },
  "required": ["required_fields", "unnecessary_fields"]
}

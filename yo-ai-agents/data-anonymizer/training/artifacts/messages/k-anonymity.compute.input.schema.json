{
  "$id": "https://yo-ai.ai/schemas/k-anonymity.compute.input.schema.json",
  "title": "K-Anonymity.Compute.Input",
  "description": "Input messageType for the K-Anonymity.Compute capability.",
  "type": "object",
  "properties": {
    "dataset": {
      "type": "array",
      "description": "Tabular dataset represented as an array of objects.",
      "items": { "type": "object", "additionalProperties": true }
    },
    "quasi_identifiers": {
      "type": "array",
      "description": "Attributes used to form equivalence classes.",
      "items": { "type": "string" }
    },
    "sensitive_attributes": {
      "type": "array",
      "description": "Sensitive attributes used for l-diversity and t-closeness calculations.",
      "items": { "type": "string" }
    },
    "parameters": {
      "type": "object",
      "description": "Optional computation parameters (e.g., minimum k threshold).",
      "additionalProperties": true
    }
  },
  "required": ["dataset", "quasi_identifiers"]
}

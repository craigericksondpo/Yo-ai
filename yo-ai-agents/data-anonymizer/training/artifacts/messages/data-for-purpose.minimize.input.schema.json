{
  "$id": "https://yo-ai.ai/schemas/data-for-purpose.minimize.input.schema.json",
  "title": "Data-For-Purpose.Minimize.Input",
  "description": "Input messageType for the Data-For-Purpose.Minimize capability.",
  "type": "object",
  "properties": {
    "dataset_schema": {
      "type": "array",
      "description": "List of fields present in the dataset, with optional metadata.",
      "items": {
        "type": "object",
        "required": ["name"],
        "properties": {
          "name": { "type": "string" },
          "type": { "type": "string" },
          "sensitivity": {
            "type": "string",
            "enum": ["low", "medium", "high", "sensitive", "special_category"]
          }
        }
      }
    },
    "intended_purpose": {
      "type": "string",
      "description": "Declared purpose for which the dataset will be used (e.g., analytics, fraud detection, personalization)."
    },
    "regulatory_frameworks": {
      "type": "array",
      "description": "Applicable regulatory or policy frameworks (e.g., CPRA, GDPR, NIST).",
      "items": { "type": "string" }
    },
    "context": {
      "type": "object",
      "description": "Optional context from the requesting agent.",
      "additionalProperties": true
    }
  },
  "required": ["dataset_schema", "intended_purpose"]
}

{
  "$id": "https://yo-ai.ai/schemas/deidentification-guidance.publish.input.schema.json",
  "title": "Deidentification-Guidance.Publish.Input",
  "description": "Input messageType for the Deidentification-Guidance.Publish capability.",
  "type": "object",
  "properties": {
    "dataset_schema": {
      "type": "array",
      "description": "Schema of the dataset, including field names and sensitivity classifications.",
      "items": {
        "type": "object",
        "required": ["name"],
        "properties": {
          "name": { "type": "string" },
          "type": { "type": "string" },
          "sensitivity": {
            "type": "string",
            "enum": ["low", "medium", "high", "sensitive", "special_category"]
          }
        }
      }
    },
    "applied_techniques": {
      "type": "array",
      "description": "List of de-identification techniques applied to the dataset.",
      "items": { "type": "string" }
    },
    "privacy_metrics": {
      "type": "object",
      "description": "Computed privacy metrics such as k-anonymity, l-diversity, and t-closeness.",
      "additionalProperties": true
    },
    "regulatory_frameworks": {
      "type": "array",
      "description": "Regulatory or policy frameworks to align guidance with (e.g., CPRA, GDPR, HIPAA, NIST).",
      "items": { "type": "string" }
    },
    "audience": {
      "type": "string",
      "description": "Intended audience for the guidance (e.g., engineers, privacy officers, regulators)."
    },
    "context": {
      "type": "object",
      "description": "Optional context from the requesting agent.",
      "additionalProperties": true
    }
  },
  "required": ["dataset_schema", "applied_techniques", "regulatory_frameworks"]
}

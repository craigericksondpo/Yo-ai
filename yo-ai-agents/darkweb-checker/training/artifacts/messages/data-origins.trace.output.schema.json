{
  "$id": "https://yo-ai.ai/schemas/data-origins.trace.output.schema.json",
  "title": "Data-Origins.Trace.Output",
  "description": "Output messageType for the Data-Origins.Trace capability.",
  "type": "object",
  "properties": {
    "suspected_origin": {
      "type": "string",
      "description": "Organization most likely responsible for leaking or mishandling the data."
    },
    "confidence": {
      "type": "string",
      "description": "Confidence level in the origin inference.",
      "enum": ["low", "medium", "high"]
    },
    "supporting_indicators": {
      "type": "array",
      "description": "Indicators used to infer the origin (e.g., schema match, field patterns, vendor signatures).",
      "items": { "type": "string" }
    }
  },
  "required": ["suspected_origin", "confidence"]
}

{
  "$id": "https://yo-ai.ai/schemas/promo-eligibility.verify.input.schema.json",
  "title": "Promo-Eligibility.Verify.Input",
  "description": "Input messageType for the Promo-Eligibility.Verify capability.",
  "type": "object",
  "properties": {
    "promotion_id": {
      "type": "string",
      "description": "Identifier for the promotion being verified."
    },
    "social_handles": {
      "type": "array",
      "description": "User social media handles to check.",
      "items": { "type": "string" }
    },
    "required_actions": {
      "type": "array",
      "description": "Actions required for eligibility (e.g., follow, hashtag, share).",
      "items": { "type": "string" }
    },
    "context": {
      "type": "object",
      "additionalProperties": true
    }
  },
  "required": ["promotion_id", "required_actions"]
}

{
  "$id": "https://yo-ai.ai/schemas/complaint.submit.output.schema.json",
  "title": "Complaint.Submit.Output",
  "description": "Output messageType for the Complaint.Submit capability.",
  "type": "object",
  "properties": {
    "submission_id": {
      "type": "string",
      "description": "Unique identifier for the submission event."
    },
    "status": {
      "type": "string",
      "description": "Status of the submission process (e.g., 'submitted', 'failed', 'queued')."
    }
  },
  "required": ["submission_id", "status"]
}

{
  "$id": "https://yo-ai.ai/schemas/complaint.publish.input.schema.json",
  "title": "Complaint.Publish.Input",
  "description": "Input messageType for the Complaint.Publish capability.",
  "type": "object",
  "properties": {
    "complaint_id": {
      "type": "string",
      "description": "Identifier of the complaint to be published."
    },
    "audience": {
      "type": "array",
      "description": "List of audiences or stakeholder groups to publish the complaint to.",
      "items": { "type": "string" }
    }
  },
  "required": ["complaint_id"]
}
